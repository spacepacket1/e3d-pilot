# e3d-pilot

Repo-agnostic orchestration scaffold for unattended repo work.

Current phase implements the CLI skeleton, config schema, and run-id plumbing.
See `docs/build-e3d-pilot.md` for the build plan and `bin/e3d-pilot --help` for
the current command surface.

